#include "main.h"
/**
*add - function that adds two integers together
* @a: takes in an integers
* @b: takes in an integers
*Return: a + b result
*/
int add(int a, int b)

{
    return (a + b);
    
}
