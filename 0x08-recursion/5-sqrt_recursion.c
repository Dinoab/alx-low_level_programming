#include "main.h"
/**
 * _sqrt_recursion - square root
 * @n: integer
 * Return: value squeare
 */
int _sqrt_recursion(int n)
{
	if (a * a > b)
		return (-1);
	else if (a * a == b)
		return (a);
	else
		return (_sqrt2(a += 1, b));
}
